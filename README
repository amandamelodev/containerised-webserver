*docker-container-with-nginx-php-mariadb-and-reddis*
Put the codebase in ./src
run docker-compose up -d
Access it on localhost
docker-compose stop - stop all containers
docker-compose down - remove all containers
docker exec -ti app bash - gets inside php container

Entrypoint: /var/www/
